# Shared components

This folder contains reusable visual components. The current dashboard uses the `ui/` button primitive; most page-specific components deliberately remain in `app/page.tsx` because this is a compact demo rather than a general component library.
