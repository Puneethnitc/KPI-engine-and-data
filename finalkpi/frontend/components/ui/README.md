# UI primitives

`button.tsx` is the shared button primitive built with class-variance-authority. Keep primitives presentation-focused; evidence logic and KPI semantics belong in the Python engine, not this folder.
