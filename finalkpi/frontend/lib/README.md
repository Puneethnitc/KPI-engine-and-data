# Shared utilities

`utils.ts` provides class-name merging helpers for the UI. It contains no KPI calculations or data-access rules; keeping those in the engine prevents the dashboard from creating a second source of business logic.
