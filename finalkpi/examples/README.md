# Example inputs

`review_windows.yaml` lists the provisional review windows used by the benchmark. `verification_design_inconclusive.yaml` is a predeclared observational DiD example designed to demonstrate an inconclusive result. Neither file is a source of ground-truth causal labels.
