# Test suite

The test suite uses Python `unittest` and contains regression coverage for access control, contracts, normalization, detection, accounting bridges, reconciliation, correlation labelling, observational verification, narrative grounding, feedback, contribution scenarios, and the jury report.

Run it from `finalkpi/` with `.venv/bin/python -m unittest discover -s tests -v`. The current verified baseline is 93 passing tests.
